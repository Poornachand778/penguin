---
summary: "CLI reference for `penguin uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `penguin uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
penguin uninstall
penguin uninstall --all --yes
penguin uninstall --dry-run
```
